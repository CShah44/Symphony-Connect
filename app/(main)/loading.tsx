import { Skeleton } from "@/components/ui/skeleton";

// todo add loading to all server side pages
const loading = () => {
  return <div className="grid place-items-center h-screen">LOADING..</div>;
};

export default loading;
